# Loaded by `vite --mode selfhosted` (npm run dev:selfhosted / build:selfhosted).
# Fully-offline build: no automatic external network contact, cloud UI hidden.
# To self-host the API too, additionally set VITE_TEMPLATE_API_URL=https://your-api.example
VITE_SELF_HOSTED=true
