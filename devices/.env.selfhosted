# Loaded by `vite --mode selfhosted` (npm run dev:selfhosted / build:selfhosted).
# Points the devices site at the ClearAlan API instead of the upstream default.
VITE_API_URL=https://api.cadesign.clearalan.ca
